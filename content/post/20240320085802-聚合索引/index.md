---
title: "聚合索引"
slug: 聚合索引
date: 2024-03-20T08:58:03+08:00
draft: true
---

<!--more-->

使用聚合索引的好处：

1. 避免mysql的索引优化：where中每个字段都有索引，但只用了一个
2. 索引下推，where中的条件在索引层即可完成过滤
3. group by操作，如果group的是索引字段，可直接在索引上操作，不需要回表。松散索引和紧凑索引。
4. 多表join，mysql是用驱动表的数据查匹配表的数据，如果join列在匹配表的索引上，则可直接拿到匹配表的索引id，不需要回表过滤。